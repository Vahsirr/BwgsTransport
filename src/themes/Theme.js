import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#F3D646',
    backgroundColor: '#FFF7D6',
    backgroundColorSecond: "#F5E8A8",
    textColor: '#000',
    accent: '#03dac4',
  },
};

export default theme;
